import { SessionsOnDayPipe } from './sessions-on-day.pipe';

describe('SessionsOnDayPipe', () => {
  it('create an instance', () => {
    const pipe = new SessionsOnDayPipe();
    expect(pipe).toBeTruthy();
  });
});
